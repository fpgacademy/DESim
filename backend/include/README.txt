In the modelsim distribution, there is a file called vpi_user.h and one
called vpi_compatibility.h. They could be find in
$MODELSIM_INSTALL_DIR/include/.

Please copy your vpi_user.h and vpi_compatibility.h files into this
directory.
