In the QuestaSim distribution, there are files called vpi_user.h and 
vpi_compatibility.h. They are found in $QUESTASIM_INSTALL_DIR/include/.

Copy the vpi_user.h and vpi_compatibility.h files into this directory.
