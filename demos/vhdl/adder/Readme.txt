The circuit produces the 5-bit Sum = X + Y + Cin, which is displayed
on LED(4-0) In this demo, X = SW(3-0), Y = SW(7-4), and Cin = SW(9).

To use:
-- set desired values to SW(9) and SW(7-0)
-- examine the result of the addition on the LEDs
