To use this demo:

-- set a value, N, to be accumulated using SW[4:0]
-- set the number of accumulations, A, wanted using SW[9:5]

-- pulse KEY[0] down/up

The circuit adds the N to itself A times (Result = N x A),
which is displayed on the LEDR lights
